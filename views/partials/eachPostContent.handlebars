
<div class="card p-2 m-2 bg-secondary d-flex">
        <div class="card-header bg-secondary float-left">
          <a href="/posts/{{posts.id}}" class = "text-decoration-none">   
            <h5 class="card-title text-dark ">{{posts.title}}</h5>  
            </a>
            <div class = "d-flex justify-content-between">
                <h6 class="card-subtitle mb-2 text-light float-left d-inline-flex"> Posted by: {{users.name}}</h6>
                <h6 class="card-subtitle mb-2 text-dark float-right d-inline-flex"> Uploaded on: {{posts.date_created}}</h6>
            </div>

        </div>
        
        <div class="card-body bg-light rounded">

          <p class="card-text">{{posts.content}}</p>
        </div>
      </div>

